import { YearsIteratorPipe } from './years-iterator.pipe';

describe('YearsIteratorPipe', () => {
  it('create an instance', () => {
    const pipe = new YearsIteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
