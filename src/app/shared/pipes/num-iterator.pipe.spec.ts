import { NumIteratorPipe } from './num-iterator.pipe';

describe('NumIteratorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumIteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
